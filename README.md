# 光伏+预测 demo

软件基于 `Gradio` 框架.

## 项目结构

- `app.ipynb`: 软件界面代码
- `app.py`: 同上, 软件界面代码 (暂时为空)
  > 这两个文件是一回事
- `Weather/get_weather.py`: 获取实时天气数据
-  `Test` 文件夹: 各种测试 忽略即可
-  `README.md`: 说明文件

## 使用方法

直接运行 `app.ipynb` 即可.

`app.py` 暂时为空, 后续会补充.



